%h2= @device.name
%p= link_to "Edit this device", edit_room_device_path(@device)

%ul
  - @groups.each do |group|
    %h3= group.name
    - group.fields.each do |field_name|
      %p 
        = "#{field_name}: "
        = @details[group.name][field_name]
  